.ie \n(.g .ds Aq \(aq
.el .ds Aq '
.TH tapectl-cartridge-relabel 1  "tapectl-cartridge-relabel " 
.SH NAME
tapectl\-cartridge\-relabel \- Correct a cartridge\*(Aqs barcode label
.SH SYNOPSIS
\fBtapectl cartridge relabel\fR [\fB\-\-json\fR] [\fB\-\-dry\-run\fR] [\fB\-v\fR|\fB\-\-verbose\fR] [\fB\-y\fR|\fB\-\-yes\fR] [\fB\-\-config\fR] [\fB\-\-home\fR] [\fB\-h\fR|\fB\-\-help\fR] <\fIBARCODE\fR> <\fINEW_BARCODE\fR> 
.SH DESCRIPTION
Correct a cartridge\*(Aqs barcode label
.PP
ADR\-0012: a cartridge\*(Aqs identity is its chip serial; the barcode is a relabelable sticker. This is a Tier 1 label correction under ADR\-0008, not a destructive act — no prompt, no `\-\-force`, no status gate — and it is the remedy `volume init`\*(Aqs auto\-register refuses with when the loaded medium\*(Aqs serial collides with an already\-registered barcode.
.SH OPTIONS
.TP
\fB\-\-json\fR
Output in JSON format
.TP
\fB\-\-dry\-run\fR
Show what would be done without making changes
.TP
\fB\-v\fR, \fB\-\-verbose\fR
Enable verbose output
.TP
\fB\-y\fR, \fB\-\-yes\fR
Skip ADR\-0008 Tier\-2 confirmation prompts. It never reaches a Tier\-3 refusal — those are facts, not risks to accept (issue #147)
.TP
\fB\-\-config\fR \fI<CONFIG>\fR
Path to config file.

NOTE: on its own this ALSO relocates the whole tapectl home to the config file\*(Aqs parent directory — database, keys, staging, receipts. That is how every test harness gets an isolated home, so it still works, but it is surprising enough that it now warns. Use \-\-home when you mean "operate on a different archive", and \-\-config only to point at a config file inside that home (issue #109).
.TP
\fB\-\-home\fR \fI<HOME>\fR
tapectl home directory: database, keys, catalogs, receipts, logs.

Defaults to ~/.tapectl. The config file is taken from <home>/config.toml unless \-\-config overrides it. Also settable as TAPECTL_HOME (issue #109).
.TP
\fB\-h\fR, \fB\-\-help\fR
Print help (see a summary with \*(Aq\-h\*(Aq)
.TP
<\fIBARCODE\fR>
Current barcode
.TP
<\fINEW_BARCODE\fR>
New barcode
